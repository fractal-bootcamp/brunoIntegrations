// We will code components to getAll elements and to query them
// the getAll elements will be a button/option that states:

// Mailing lists
// Contacts
// Messages

//Inside each of them:
// Get all
// Get by Id
// Get by name (message by 'subject')
// Get by word